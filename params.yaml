data_ingestion:
  test_size: 0.2

feature_engineering:
  max_features: 30000   # increased from typical 5000-10000 for better coverage
  ngram_max: 2          # unigrams + bigrams (NEW — handles negation like "not good")
  min_df: 3             # ignore terms seen in fewer than 3 documents (NEW)